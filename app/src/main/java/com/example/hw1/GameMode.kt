package com.example.hw1

/**
 * All game modes passed from MenuActivity to GameActivity.
 * SENSOR mode uses tilt forward/back to change speed dynamically.
 */
enum class GameMode {
    BUTTON_SLOW,
    BUTTON_FAST,
    SENSOR
}
